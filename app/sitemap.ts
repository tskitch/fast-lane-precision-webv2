import type { MetadataRoute } from "next"; const routes=["","/capabilities","/capabilities/milling","/capabilities/turning","/capabilities/swiss","/quality","/industries","/about","/contact","/privacy"]; export default function sitemap():MetadataRoute.Sitemap{return routes.map(route=>({url:`https://fastlaneprecision.com${route}`,lastModified:new Date(),changeFrequency:route===""?"monthly":"yearly",priority:route===""?1:.7}));}
